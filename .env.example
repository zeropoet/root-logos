# Prefer the four durable OAuth 1.0a user-context credentials for unattended
# operation. The OAuth 2.0 bearer token remains available for manual use.
# Never commit real credentials.
X_API_KEY=
X_API_SECRET=
X_ACCESS_TOKEN=
X_ACCESS_TOKEN_SECRET=
X_USER_ACCESS_TOKEN=
